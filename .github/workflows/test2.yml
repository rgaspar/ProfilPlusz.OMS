name: Message2

on:
  workflow_run:
    workflows: ["Message1"]
    types: [completed]


jobs:
  secondstep:
    runs-on: ubuntu-latest
    steps:
      - name: Message 2
        run: echo 'Message 2'